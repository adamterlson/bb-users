const { Model } = require('backbone');

class GroupModel extends Model {
  constructor(options) {
    super(options);

    // Properties
    
  }

  // Backbone Overrides

  // Backbone Events

  // API Methods

  // Private
}

export default GroupModel;
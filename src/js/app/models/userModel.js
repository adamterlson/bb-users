const { Model } = require('backbone');

class UserModel extends Model {
  constructor(options) {
    super(options);

    // Properties
    
  }

  // Backbone Overrides

  // Backbone Events

  // API Methods

  // Private
}

export default UserModel;